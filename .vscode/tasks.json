{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "zip",
            "type": "shell",
            "command": "zip -r ../bloque.zip data/ pack.mcmeta",
            "windows": {
                "command": "wsl zip -r ../bloque.zip data/ pack.mcmeta"
            }
        }
    ]
}
